SEARCH PROTOCOL (v1.1)
=======================

1. SEARCH CLASSIFICATION:
   - Level 1: Quick Web Search (DuckDuckGo generic)
   - Level 2: Business Registry Search (YouControl, Opendatabot, Clarity)
   - Level 3: Open Data Portal Search (data.gov.ua)
   - Level 4: Headless Browser Scraping (Puppeteer - default for speed)
   - Level 5: Deep DOM/Protocol Interaction (Chrome-DevTools - for persistence, auth, and debugging)
   - Level 6: Technical Documentation & Library Search (context7)
   - Level 7: Geographic & Visual Location Search (xcodebuild/maps_*)
   - Level 8: Cadastral Search (Держгеокадастр, Kadastr.live, WFS/WMS services)

2. BROWSER TOOL SELECTION:
   - **duckduckgo-search**: Use for rapid factual lookups without navigating full pages.
   - **puppeteer**: Use for standard web scraping, navigating SPAs, and basic interaction. Fastest browser option.
   - **chrome-devtools**: Use for high-precision tasks: capturing console logs, monitoring network traffic, bypass attempts, or when extreme DOM stability is needed.
   - **xcodebuild**: Use for NATIVE browser interactions (managing tabs, reading URLs), visual/GUI verification, AND all location-based search (maps_* tools), Street View, traffic analysis, coordinates. Unified hub for all macOS + Maps tools.

3. EXECUTION GUIDELINES:
   - START with specialized tools (Level 2/3) for technical data (ЄДРПОУ, addresses, owners).
   - USE xcodebuild maps_* tools for address verification or location context BEFORE deep web scraping.
   - PREFER macos-use_fetch_url (via xcodebuild) for clean data extraction from articles/registries.
   - USE Puppeteer/DevTools ONLY for interactive pages (clicks, forms, dynamic content).
   - IF BLOCKED by robot detection, switch to Chrome-DevTools for more human-like emulation or use xcodebuild GUI tools to manually guide.
   - FOR CADASTRAL: Use OAuth2 API (e.land.gov.ua) for structured queries, WFS/WMS for vector data, data.gov.ua for GeoJSON datasets.

4. DATA RETENTION:
   - All found datasets (.csv, .xlsx, .json, .geojson, .shp) MUST be saved to the workspace folder.
   - Extract key "seed" metadata (dates, IDs, names, cadastral numbers) to the Knowledge Graph to enable recursive searching.
   - FOR CADASTRAL: Store GeoJSON/SHP datasets in vault, preserve coordinate systems and attribute schemas.

5. RECURSIVE SEARCH DOCTRINE:
   - After finding initial data, ALWAYS analyze if more precise terms (IDs, names) can be used for a second, deeper search.

6. OFFICIAL SOURCE DIRECTIVE:
   - Whenever possible, PRIORITIZE official instructions, manuals, or API documentation for the systems you are searching.
   - USE these official sources to refine your search queries and data extraction logic (e.g. finding hidden API endpoints or specific URL patterns).

7. SEMANTIC CHAINING DIRECTIVE:
   - Upon finding a new dataset, ALWAYS analyze column names for potential "join keys" (IDs, names, emails).
   - USE `trace_data_chain` to recursively reconstruct a complete profile from fragmented sources if you identify a semantic anchor.
   - If a link is found, DOCUMENT it in the Knowledge Graph for future automated discovery.

8. TEMPORAL ACQUISITION DOCTRINE (2020-2026):
   - **Current Horizon**: The current system year is 2026. Prioritize data from 2026, 2025, and late 2024 for immediate relevance.
   - **Tiered Deepening**: If a search for "{query} 2026" or "{query} 2025" yields zero or insufficient results, recursively deepen the search into 2024, 2023, 2022, 2021, and 2020.
   - **Verification**: ALWAYS verify the publication date of a dataset to ensure it matches the requested temporal slice.
   - **Cross-Referencing**: When a gap is found in 2026 data, seek older versions (2025-2020) and use them to reconstruct current state if official updates are pending.

9. MACHINE-READABLE CONFIGURATION (JSON):
{
  "search_rules": {
    "business": {
      "operator": "(site:youcontrol.com.ua OR site:opendatabot.ua OR site:clarity-project.info) \"{query}\"",
      "priority_domains": ["youcontrol.com.ua", "opendatabot.ua", "clarity-project.info"]
    },
    "open_data": {
      "operator": "site:data.gov.ua \"{query}\" (2020 OR 2021 OR 2022 OR 2023 OR 2024 OR 2025 OR 2026) filetype:csv OR filetype:xlsx OR filetype:json",
      "priority_domains": ["data.gov.ua"]
    },
    "structured_data": {
      "operator": "site:data.gov.ua \"{query}\" (2020 OR 2021 OR 2022 OR 2023 OR 2024 OR 2025 OR 2026) filetype:csv OR filetype:xlsx OR filetype:json",
      "priority_domains": ["data.gov.ua"]
    },
    "cadastral": {
      "operator": "(site:e.land.gov.ua OR site:data.gov.ua) \"{query}\" filetype:geojson OR filetype:shp OR tags:кадастр",
      "priority_domains": ["e.land.gov.ua", "data.gov.ua", "kadastr.live", "map.land.gov.ua"],
      "api_endpoints": ["https://e.land.gov.ua/api/", "http://map.land.gov.ua/geowebcache/service/wms"],
      "oauth_required": ["e.land.gov.ua"],
      "rate_limits": "100-500 requests/minute depending on tariff"
    },
    "court": {
      "operator": "(site:reyestr.court.gov.ua OR site:opendatabot.ua/court OR site:court.gov.ua) \"{query}\" (2020 OR 2021 OR 2022 OR 2023 OR 2024 OR 2025 OR 2026)",
      "priority_domains": ["reyestr.court.gov.ua", "opendatabot.ua", "court.gov.ua"]
    }
  },
  "temporal_horizon": {
    "current_year": 2026,
    "primary_range": [2024, 2026],
    "deep_range": [2020, 2023]
  }
}

DEVIATION POLICY (ROOM TO MANEUVER)
===================================
- VECTOR GUIDANCE: Search levels are a "starting vector". You are encouraged to skip levels or pivot search strategies immediately if your intuition (or initial results) suggests a better path.
- AUTONOMY: Professional search quality and GOAL achievement always override rigid adherence to protocol hierarchy.
