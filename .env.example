# AtlasTrinity Environment Configuration
# API Keys Only

# === SYSTEM ===
NODE_ENV=development
PORT=8000
LOG_LEVEL=INFO
PRODUCTION=false
PYTHONPATH=src

# === LLM PROVIDER ===
# LLM_PROVIDER=copilot                # copilot | windsurf (or set in config.yaml models.provider)

# === API KEYS (Required) ===
MISTRAL_API_KEY=your_mistral_key_here
COPILOT_API_KEY=your_key_here
GITHUB_TOKEN=your_token_here
GITHUB_TOKEN_ACTION=your_action_token_here
VISION_API_KEY=your_key_here
OPENROUTER_API_KEY=your_openrouter_key_here

# === Windsurf/Codeium (Alternative provider) ===
# WINDSURF_API_KEY=sk-ws-...          # Get via: python tools/get_windsurf_token.py --key-only
# WINDSURF_INSTALL_ID=                # Installation ID from Windsurf DB
# WINDSURF_MODEL=deepseek-v3          # Free models: deepseek-v3, deepseek-r1, swe-1.5, kimi-k2.5
# WINDSURF_PROXY_URL=http://127.0.0.1:8085  # Local proxy URL (scripts/windsurf_proxy.py)
# WINDSURF_DIRECT=false               # Set "true" to bypass proxy, call API directly
# WINDSURF_MODE=proxy                 # Force mode: local | proxy | direct | cascade
# WINDSURF_API_SERVER=https://server.self-serve.windsurf.com
# WINDSURF_LS_PORT=                   # Language Server port (auto-detected)
# WINDSURF_LS_CSRF=                   # Language Server CSRF token (auto-detected)

# === DATABASE (Optional Override) ===
# MCP_POSTGRES_URL=postgresql://localhost/postgres
REDIS_URL=redis://localhost:6379/0

# === Browser Automation ===
# PUPPETEER_ALLOW_DANGEROUS=false     # Allow dangerous Puppeteer operations

# === Google Maps Configuration ===
GOOGLE_MAPS_API_KEY=
VITE_GOOGLE_MAPS_API_KEY=             # Frontend Google Maps key (Vite)
